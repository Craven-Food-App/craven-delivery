# Chat backend

1. Create venv and install: `pip install -r requirements.txt`
2. Run: `python app.py`
3. Server runs on http://localhost:5000. Start the chat frontend and connect; you’ll choose your username in the form before the widget appears.
