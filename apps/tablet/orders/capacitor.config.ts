import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.craven.delivery.tablet.orders",
  appName: "Craven Live Orders",
  webDir: "dist-build"
};

export default config;
